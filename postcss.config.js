module.exports = {
  // Minimal local PostCSS config to avoid loading a global/tailwind config.
  // Keep empty or add plugins as needed by the project.
  plugins: [],
};
module.exports = {
  // empty PostCSS config to avoid loading a global config that may require Tailwind
  plugins: [],
};
